[package]
name = "proto-crate-gen"
version = "0.1.0"
authors = ["Einride"]
description = "Generate Rust crate from trees of probobuf files"
edition = "2018"

[dependencies]
walkdir = "2.3.1"
tonic-build = {version = "0.4.0", default-features = false, features = ["rustfmt", "prost", "transport"] }
prost-build = "0.7.0"
codegen = "0.1.3"
anyhow = "1.0.38"
structopt = "0.3.21"

[dev-dependencies]
tempdir = "0.3.7"
